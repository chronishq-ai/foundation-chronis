# Pinned dependencies for chronis_ml / HARDENERS sprint 13-15.
# Every entry must stay pinned (==) — ops.py::pins() enforces this.
# licenses.json must list every package here with an exact version match.
numpy==1.26.4
scipy==1.13.1
mlflow==2.14.3
pytest==8.3.2
python-dotenv==1.0.1
